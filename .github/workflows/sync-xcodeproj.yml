name: Sync Generated Xcode Project

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - "native-ios/project.yml"
      - "native-ios/ListenSDR/**"
      - ".github/workflows/sync-xcodeproj.yml"

permissions:
  contents: write

jobs:
  sync-xcodeproj:
    runs-on: macos-15
    concurrency:
      group: sync-xcodeproj-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: |
          cd native-ios
          xcodegen generate

      - name: Commit generated xcodeproj if changed
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- native-ios/ListenSDR.xcodeproj; then
            echo "No changes in generated xcodeproj."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add native-ios/ListenSDR.xcodeproj
          git commit -m "chore(ios): sync generated xcodeproj"
          git push
