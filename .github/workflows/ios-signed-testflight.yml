name: iOS Signed IPA + TestFlight (Native)

on:
  workflow_dispatch:
    inputs:
      upload_to_testflight:
        description: "Upload exported IPA to App Store Connect TestFlight"
        required: true
        default: false
        type: boolean

jobs:
  build-signed-ipa:
    runs-on: macos-15
    permissions:
      contents: read
    concurrency:
      group: native-ios-signed-${{ github.ref }}
      cancel-in-progress: true
    env:
      IOS_BUNDLE_ID: com.kazek.sdr
      IOS_SCHEME: ListenSDR
      IOS_PROJECT: native-ios/ListenSDR.xcodeproj
      ARCHIVE_PATH: native-ios/build/ListenSDR.xcarchive
      EXPORT_PATH: native-ios/build/export
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      ASC_API_KEY_BASE64: ${{ secrets.ASC_API_KEY_BASE64 }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      IOS_DIST_CERT_P12_BASE64: ${{ secrets.IOS_DIST_CERT_P12_BASE64 }}
      IOS_DIST_CERT_PASSWORD: ${{ secrets.IOS_DIST_CERT_PASSWORD }}
      IOS_PROVISION_PROFILE_BASE64: ${{ secrets.IOS_PROVISION_PROFILE_BASE64 }}
      KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Resolve temp paths
        shell: bash
        run: |
          echo "KEYCHAIN_PATH=$RUNNER_TEMP/listen-sdr-signing.keychain-db" >> "$GITHUB_ENV"

      - name: Validate required secrets
        shell: bash
        run: |
          set -euo pipefail
          missing=0
          for var in \
            APPLE_TEAM_ID \
            ASC_KEY_ID \
            ASC_ISSUER_ID \
            ASC_API_KEY_BASE64; do
            if [ -z "${!var:-}" ]; then
              echo "::error::Missing required secret: $var"
              missing=1
            fi
          done
          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

      - name: Resolve signing mode
        shell: bash
        run: |
          set -euo pipefail
          if [ -n "${IOS_DIST_CERT_P12_BASE64:-}" ] && \
             [ -n "${IOS_DIST_CERT_PASSWORD:-}" ] && \
             [ -n "${IOS_PROVISION_PROFILE_BASE64:-}" ] && \
             [ -n "${KEYCHAIN_PASSWORD:-}" ]; then
            echo "SIGNING_MODE=manual" >> "$GITHUB_ENV"
            echo "EXPORT_SIGNING_STYLE=manual" >> "$GITHUB_ENV"
            echo "EXPORT_METHOD=app-store" >> "$GITHUB_ENV"
            echo "Using manual signing from repository secrets."
          else
            echo "SIGNING_MODE=automatic" >> "$GITHUB_ENV"
            echo "EXPORT_SIGNING_STYLE=automatic" >> "$GITHUB_ENV"
            echo "EXPORT_METHOD=app-store-connect" >> "$GITHUB_ENV"
            echo "Manual signing secrets are incomplete. Falling back to automatic Xcode signing with ASC auth."
          fi

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: |
          cd native-ios
          xcodegen generate

      - name: Setup App Store Connect API key
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.appstoreconnect/private_keys"
          echo "$ASC_API_KEY_BASE64" | base64 --decode > "$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8"
          chmod 600 "$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8"

      - name: Setup signing keychain and certificate
        if: ${{ env.SIGNING_MODE == 'manual' }}
        shell: bash
        run: |
          set -euo pipefail
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH"
          echo "$IOS_DIST_CERT_P12_BASE64" | base64 --decode > "$RUNNER_TEMP/dist.p12"
          security import "$RUNNER_TEMP/dist.p12" \
            -k "$KEYCHAIN_PATH" \
            -P "$IOS_DIST_CERT_PASSWORD" \
            -T /usr/bin/codesign \
            -T /usr/bin/security \
            -T /usr/bin/xcodebuild
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

      - name: Install provisioning profile
        if: ${{ env.SIGNING_MODE == 'manual' }}
        id: profile
        shell: bash
        run: |
          set -euo pipefail
          PROFILE_DIR="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PROFILE_DIR"
          PROFILE_PATH="$RUNNER_TEMP/profile.mobileprovision"
          PROFILE_PLIST="$RUNNER_TEMP/profile.plist"
          echo "$IOS_PROVISION_PROFILE_BASE64" | base64 --decode > "$PROFILE_PATH"
          security cms -D -i "$PROFILE_PATH" > "$PROFILE_PLIST"

          PROFILE_UUID=$(/usr/libexec/PlistBuddy -c "Print UUID" "$PROFILE_PLIST")
          PROFILE_NAME=$(/usr/libexec/PlistBuddy -c "Print Name" "$PROFILE_PLIST")

          cp "$PROFILE_PATH" "$PROFILE_DIR/$PROFILE_UUID.mobileprovision"
          echo "profile_name=$PROFILE_NAME" >> "$GITHUB_OUTPUT"
          echo "profile_uuid=$PROFILE_UUID" >> "$GITHUB_OUTPUT"

      - name: Archive signed app
        if: ${{ env.SIGNING_MODE == 'manual' }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf native-ios/build
          xcodebuild \
            -project "$IOS_PROJECT" \
            -scheme "$IOS_SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$ARCHIVE_PATH" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            PROVISIONING_PROFILE_SPECIFIER="${{ steps.profile.outputs.profile_name }}" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            PRODUCT_BUNDLE_IDENTIFIER="$IOS_BUNDLE_ID" \
            OTHER_CODE_SIGN_FLAGS="--keychain $KEYCHAIN_PATH" \
            clean archive | tee native-ios-build-signed.log

      - name: Archive signed app automatically
        if: ${{ env.SIGNING_MODE == 'automatic' }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf native-ios/build
          xcodebuild \
            -project "$IOS_PROJECT" \
            -scheme "$IOS_SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$ARCHIVE_PATH" \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8" \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            CODE_SIGN_STYLE=Automatic \
            PRODUCT_BUNDLE_IDENTIFIER="$IOS_BUNDLE_ID" \
            clean archive | tee native-ios-build-signed.log

      - name: Export IPA
        shell: bash
        run: |
          set -euo pipefail
          if [ "$SIGNING_MODE" = "manual" ]; then
            cat > "$RUNNER_TEMP/exportOptions.plist" <<EOF
            <?xml version="1.0" encoding="UTF-8"?>
            <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
            <plist version="1.0">
            <dict>
              <key>method</key>
              <string>$EXPORT_METHOD</string>
              <key>signingStyle</key>
              <string>$EXPORT_SIGNING_STYLE</string>
              <key>teamID</key>
              <string>$APPLE_TEAM_ID</string>
              <key>provisioningProfiles</key>
              <dict>
                <key>$IOS_BUNDLE_ID</key>
                <string>${{ steps.profile.outputs.profile_name }}</string>
              </dict>
              <key>stripSwiftSymbols</key>
              <true/>
              <key>compileBitcode</key>
              <false/>
              <key>destination</key>
              <string>export</string>
            </dict>
            </plist>
            EOF
          else
            cat > "$RUNNER_TEMP/exportOptions.plist" <<EOF
            <?xml version="1.0" encoding="UTF-8"?>
            <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
            <plist version="1.0">
            <dict>
              <key>method</key>
              <string>$EXPORT_METHOD</string>
              <key>signingStyle</key>
              <string>$EXPORT_SIGNING_STYLE</string>
              <key>teamID</key>
              <string>$APPLE_TEAM_ID</string>
              <key>manageAppVersionAndBuildNumber</key>
              <false/>
              <key>stripSwiftSymbols</key>
              <true/>
              <key>compileBitcode</key>
              <false/>
              <key>destination</key>
              <string>export</string>
            </dict>
            </plist>
            EOF
          fi

          xcodebuild \
            -exportArchive \
            -archivePath "$ARCHIVE_PATH" \
            -exportPath "$EXPORT_PATH" \
            -exportOptionsPlist "$RUNNER_TEMP/exportOptions.plist" \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8" \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID" | tee native-ios-export.log

      - name: Resolve exported IPA path
        id: ipa
        shell: bash
        run: |
          set -euo pipefail
          IPA_PATH=$(find "$EXPORT_PATH" -maxdepth 1 -name "*.ipa" -print -quit)
          if [ -z "$IPA_PATH" ]; then
            echo "::error::No IPA exported in $EXPORT_PATH"
            exit 1
          fi
          echo "path=$IPA_PATH" >> "$GITHUB_OUTPUT"

      - name: Upload signed IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: Listen-SDR-signed-ipa
          path: ${{ steps.ipa.outputs.path }}
          if-no-files-found: error
          retention-days: 7

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Listen-SDR-native-ios-signed-build-logs
          path: |
            native-ios-build-signed.log
            native-ios-export.log
          if-no-files-found: warn
          retention-days: 7

      - name: Upload to TestFlight
        if: ${{ github.event.inputs.upload_to_testflight == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          xcrun altool \
            --upload-app \
            --type ios \
            --file "${{ steps.ipa.outputs.path }}" \
            --apiKey "$ASC_KEY_ID" \
            --apiIssuer "$ASC_ISSUER_ID"

      - name: Cleanup keychain and private key
        if: always()
        shell: bash
        run: |
          set +e
          rm -f "$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8"
          if [ "${SIGNING_MODE:-}" = "manual" ]; then
            security delete-keychain "$KEYCHAIN_PATH"
          fi
